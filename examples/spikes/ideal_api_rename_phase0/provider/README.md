# Ideal API rename spike provider

Provider package for the Phase 0 API-rename language-mechanics spike.

It defines the public types, aliases, constructors, and traits that the sibling `consumer/` package imports to verify cross-package MoonBit behavior. See the parent spike README for findings.
