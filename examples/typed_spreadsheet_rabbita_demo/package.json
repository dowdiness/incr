{
  "name": "typed-spreadsheet-rabbita-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build:moonbit": "cd ../.. && moon build --target js --release",
    "dev": "npm run build:moonbit && vite",
    "build": "npm run build:moonbit && vite build",
    "smoke": "node scripts/smoke-dist.mjs",
    "test:dom": "npm run build && node scripts/test-dom.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "1.60.0",
    "vite": "^8.0.14"
  }
}
