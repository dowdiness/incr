# Implementation Plans

Generated by the `improve` skill on 2026-07-13 against commit `31afb08`,
reconciled on 2026-07-18 against commit `3d768f3` after PR #414 merged, and
extended on 2026-07-19 against commit `f839477` by a delta audit of the code
merged since `31afb08` (PRs #400–#414 source changes; the prior deep audit
covered everything older), and extended again on 2026-07-19 with the
operator-commissioned differential-dataflow correctness spike. Completed plan
files are deleted under the documentation retention policy; Git history is the
recovery path.

Future plans are implementation handoffs, not permission to push, publish,
deploy, or open a pull request. Those actions require separate operator
instruction.

## Execution order and status

Plans 008–009 remain from the delta-audit findings selected non-interactively
by leverage (the skill's default when no operator is present to choose); the
operator may reject or reorder them. Plan 010 is separately commissioned by
the operator and is independent of both audit plans. None imposes an execution
order on the others.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 008 | Extend controlled `value` reconciliation to `<textarea>` | P2 | S | — | TODO |
| 009 | Cover mid-fixpoint disposal and rebuild-after-teardown in datalog tests | P2 | S | — | TODO |
| 010 | Build a retractable dataflow correctness spike | P1 | L | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` |
`BLOCKED (<reason>)` | `REJECTED (<reason>)`.

## Reconciliation notes

- Plan 007 was independently verified and merged in
  [PR #415](https://github.com/dowdiness/incr/pull/415) as commit `823e63f`.
  Its completed plan file was deleted; Git history recovers the execution
  record. No ADR needed: this is a small local checker bug fix guarded by
  regression tests.
- Plan 006's 17 immutable GitHub Action references, syntax checks, repository
  checks, and GitHub-hosted CI were verified after PR #414 merged. Its completed
  plan file was deleted; Git history recovers the execution record.
- R03 is complete: `CLAUDE.md` is a symlink to canonical `AGENTS.md`.
- R06 is complete: commit `b1a4531` deleted the stale alien-signals comparison
  and refreshed the surviving Salsa comparison.
- R15's timing-evidence gate has fired now that Plan 006 is complete, but no CI
  performance investigation is commissioned.
- R16 is complete: PR #408 shipped the typed-spreadsheet `incr_tea` proof and
  issue #268 is closed.
- R21 remains blocked. The repository pins MoonBit `0.10.4+ade96c819`; its
  official GitHub release still contains only `moonbit-wasm.tar.gz`, not the
  native CLI-plus-core artifacts and independent provenance required by R21.

## Recorded for later revisit

The deep audit findings not selected for implementation are retained here so a
future audit does not rediscover them without context. `RECORDED` means no
implementation is currently commissioned. `BLOCKED` means the opportunity is
valid but its explicit external evidence gate has not fired.

| ID | Opportunity | Category | Est. | Revisit trigger / next evidence | Status |
|---|---|---|---:|---|---|
| R01 | Delete stopped animation-frame loop tombstones from `globalThis.__incrTeaRafLoops` while preserving stop-during-callback race safety | bug/tests | S | Add a JS store-size stress test and a stop-during-callback case against the shared-Program mount-lifecycle baseline | RECORDED |
| R02 | Enforce committed `pkg.generated.mbti` freshness in CI with `moon info` plus a clean-diff gate | dx/tests | S | After R21's provenance gate fires and a canonical verified bootstrap lands | RECORDED |
| R03 | Replace stale `CLAUDE.md` API/package duplication with a short pointer to canonical `AGENTS.md` | docs/dx | S | `CLAUDE.md` now resolves to `AGENTS.md` through the required symlink | DONE |
| R04 | Remove the repository-authored `REQUIRED SUB-SKILL` agent directive from the shipped `docs/plans/2026-06-25-program-stateful.md` plan and prevent shipped plans from being indexed as executable | security/docs | S | Combine with R05; retain historical intent without direct agent commands | DONE |
| R05 | Rebuild `docs/roadmap.md`, `docs/todo.md`, and the `docs/README.md` Active Plans list as a target-vocabulary current backlog; archive shipped `Expr`, hooks, and `Program::stateful` work | docs | M | Maintainer chooses the one canonical current-priority document | DONE (completed-plan cleanup) |
| R06 | Refresh or explicitly mark historical the Salsa and alien-signals comparison documents, which describe shipped subscriber/effect/GC work as deferred and show removed APIs | docs | M | Commit `b1a4531` retired the alien-signals note and refreshed the Salsa note | DONE |
| R07 | Repair `incr_tea` relocation links and update the direction note to distinguish the root framework module from the `examples/incr_tea` harness | docs | S | Next `incr_tea` documentation pass | RECORDED |
| R08 | Remove or rename publicly exposed legacy `ReactiveId` in `dowdiness/incr/types` without breaking supported consumers | architecture/migration | S | After R02; first confirm whether any external deep-package consumer exists | RECORDED |
| R09 | Make the Rabbita demo's local `build:moonbit` script honor `NEW_MOON_MOD=0` and target only its own package | dx | S | Next demo-tooling cleanup | RECORDED |
| R10 | Measure the production renderer tax from unconditional derived-event recompute instrumentation | perf investigation | M | Add enabled/disabled JS/browser measurements; optimize only on a material result | RECORDED |
| R11 | Establish a real 50×50 spreadsheet interaction benchmark covering selection, typing, and commit with sparse/dense drafts | perf investigation | M | Before changing draft lookup shape, view windowing, or render boundaries | RECORDED |
| R12 | Re-measure keyed prepend/removal DOM work and compare an anchor-based minimal-move prototype | perf investigation | M | Current Chromium numbers reproduce and move counts dominate | RECORDED |
| R13 | Measure stable subscription reconciliation at 0/1/16/256 subscriptions before adding version/change gating | perf investigation | M | Stable-set scans and allocations are material in JS measurements | RECORDED |
| R14 | Benchmark disconnected Datalog graphs with 1/100/1,000 rules before considering active-rule scheduling | perf investigation | S benchmark / L fix | Whole-runtime scanning dominates a realistic workload | RECORDED |
| R15 | Measure repeated MoonBit installation/update time across CI jobs before adding caches or consolidating jobs | perf/dx investigation | S | Plan 006 is complete; use PR #414 Actions timing data and preserve failure isolation | RECORDED |
| R16 | Complete the typed-spreadsheet `incr_tea` integration as the framework acceptance driver | direction | L | PR #408 shipped the proof and closed issue #268 | DONE |
| R17 | Add namespace-aware SVG support after controlled-form correctness | direction | M | Controlled-form reconciliation is complete; remaining trigger is an editor/diagram consumer supplying acceptance cases | RECORDED |
| R18 | Run reactive-collection driver discovery in Canopy before adding any `ReactiveMap`, relation-delta, or cleanup public API to `incr` | direction/spike | M | A downstream workload proves `DerivedMap` insufficient | RECORDED |
| R19 | Explore WebComponent/custom-element mount boundaries as a spike, not a stability commitment | direction/spike | M–L | The editor driver demonstrates concrete embedding and teardown pressure | RECORDED |
| R20 | Retire or refresh `docs/research/next-sessions-runtime-roadmap.md`, whose mandatory starting point predates composable listeners and the v0.13/v0.14 boundary cleanup | docs | S–M | Combine with R05 or revalidate across Loom and Canopy | DONE |
| R21 | Replace the mutable MoonBit installer/setup path with an independently verified native CLI-plus-core bootstrap | security/ci | M | Official MoonBit publishes durable versioned native CLI and core artifacts plus immutable vendor-published digests, signatures, or attestations for the repository's pinned version; do not substitute mutable `latest` checksums, runtime-computed digests, or the WASM compiler release | BLOCKED (vendor provenance) |
| R22 | Bound the `ViewUnchanged` controlled-reconcile scan (`renderer_js.mbt` `reconcile_controlled_rendered` recurses all nodes and attrs on every no-op flush) e.g. by recording controlled-subtree presence at render time | perf investigation | M | JS/browser measurement on a large mostly-static tree shows the per-flush traversal is material; `controlled_reconcile_dom_bench.mbt` is the starting harness | RECORDED |
| R23 | Decide the Scope-vs-datalog bulk-disposal contract: `Scope` disposes owned cells in registration order (`scope.mbt:108-110`), which would dispose relations before their pinning rules if datalog `CellId`s were ever adopted via `add_cell_ids` | architecture | M | Latent — no current constructor scopes datalog cells; decide (reverse-order disposal or explicit rejection of datalog ids) before scoping datalog lifecycles | RECORDED |
| R24 | AI-context operation intake: the spreadsheet demos publish a read-only `AiContextSnapshot` (globalThis accessors, evidence classification) with no matching `apply(op)` intake; a validated command intake routed through `run_batched_op` would close the agent loop. Trust boundary for agent-authored formula text must be decided first | direction/spike | L (coarse) | Maintainer wants the closed-loop AI spreadsheet driver; `bench_api.mbt`'s dispatch-from-JS harness proves the mechanism | RECORDED |

## Findings considered and rejected

- **Runtime-to-services decomposition**: rejected by the current R2 ADR. The
  runtime is a coordinator facade over already-separated engines; no second
  consumer or dependency-injection driver exists.
- **Per-mode Runtime split**: rejected by ADR. One revision clock and
  cross-strategy edges are intentional differentiators, not accidental coupling.
- **Optimize `AccumulatorCommitHook::after_abort`'s quadratic scan now**:
  rejected for current workloads. The repository's measured evidence says real
  consumers use roughly 1–2 accumulators per memo; reopen only around 50+
  accumulators with frequent aborts.
- **Treat `Derived::map_no_backdate`'s permissive read as an observer-allocation
  performance bug**: rejected after source verification. Current strict and
  permissive reads share `read_honest()` internally; the difference is the
  tracked-context guard, not an extra observer lifecycle.
- **Expose the private static/applicative Derived fast path now**: rejected by
  the accepted public-surface ADR until `Expr`, an attachment pipeline, or a
  downstream UI supplies a measured driver. The shipped dynamic `map` helpers
  do not reopen that decision.
- **Consolidate CI jobs solely because installation is duplicated**: not
  commissioned without timing evidence. R15 preserves the measurement question;
  PR #414 pins third-party action code without changing job isolation, while
  R21 separately tracks the blocked MoonBit bootstrap provenance gap.
- **`request_after_flush` per-command snapshot allocation**
  (`incr_tea/program.mbt:221-228`): the `.map` is a defensive copy against
  re-entrant registration mutation, runs only per `AfterFlush` command (not
  per flush), and is negligible. Not worth doing.
- **Machine-composition bench mutation-observer leak on assertion failure**
  (`machine_composition_dom_bench.mbt` sets the module-global
  `dom_set_property_mutation_observer` and resets it only on the success
  path): benchmark-only, affects no shipped code path. Not worth doing.

## Audit coverage and verification note

The source audit covered every active workspace package and all nine Improve
categories. Historical files under the retired archive, external Loom/Canopy
source, network dependency advisories, runtime tests, builds, and benchmarks
were not executed during the read-only audit.

This reconciliation verified the merged Plan 006 action inventory, `actionlint`,
documentation boundaries, PR #414's merged state, and its GitHub-hosted checks.
The full MoonBit suite was not rerun because reconciliation changes only plan
metadata and deletes the completed plan record.

The 2026-07-19 delta audit (standard level) covered only source changed in
`31afb08..f839477`: `incr/cells` datalog lifecycle + scope + kernel fixpoint,
`incr_tea` renderer/program/controlled-forms/machine-composition, the
spreadsheet-demo AI-context code, changed CI workflows, and the new
documentation-boundary scripts. It did not re-audit unchanged code, execute
tests/builds/benchmarks, or audit external Loom/Canopy source. Key vetted
verifications: the `urlsplit` scheme misparse behind Plan 007 was reproduced
directly; the `<textarea>` exclusion behind Plan 008 was confirmed at
`renderer_js.mbt:581`/`:723` with reachability via the public
`node(tag, ...)` constructor; the delta's CI workflow steps preserved PR
#414's SHA pinning, and no injection sinks or secrets were found in the
changed code.
