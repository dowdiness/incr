# Ideal API rename spike consumer

Consumer package for the Phase 0 API-rename language-mechanics spike.

It imports the sibling `provider/` package and checks how aliases, deprecated constructors, trait impls, and same-receiver methods behave across a package boundary. See the parent spike README for findings.
