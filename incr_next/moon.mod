name = "dowdiness/incr_next"

version = "0.1.0-alpha.1"

readme = "README.mbt.md"

repository = "https://github.com/dowdiness/incr"

license = "Apache-2.0"

description = "Pre-1.0 typed incremental query kernel"
