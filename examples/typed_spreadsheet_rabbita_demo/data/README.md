# Typed spreadsheet data export

Small JS-target package that exports the fixed typed spreadsheet scenario as JSON without mounting the Rabbita UI.

The editable browser demo does not use this package for rendering; it remains available for tests or external demos that need MoonBit-computed scenario data.
